+++
title = '流量、样本解密'
date = 2024-08-20T15:03:29+08:00
draft = false
+++
## 0x1、概述

在研究学习Windows程序逆向的时候，遇到文件加解密。加解密比价贴合实际工作，特别是APT的二次下载样本、shellcode、C&C等又如键盘记录、屏幕截图等，网络传输数据流都有很大概率是加密的。

如下面恶意代码，从自身资源节中释放加密文件，再将释放的文件就行解密，最后将解密出来的pe文件注入到svchost进程中。

![image-20240914143319962](https://cdn.jsdelivr.net/gh/dshh-sec/mypic@img/img/202409201646599.png)

## 0x2、加解密

一般简单的加密算法识别，可以从文件结构入手，其中最重要的就是文件头，文件头是已知的数据，可以使用文件头进行算法猜测或者爆破。

- **baes64**：填充字符，如果输入数据的长度不是 3 的倍数，Base64 编码会在编码后的结果末尾添加**一个或两个等号（=）**作为填充字符，以确保编码后的字符串长度是 4 的倍数，另外样本中可能会有标准`Base64`表。
- xor：异或加密只需要一个字符即可，单字节加密可通过暴力破解来解密（暴力破解识别）
- 凯撒：古老且简单的算法，字符串向右移动字符，偏移量是一个整数，在 0 到 25 之间，简单爆破可解。
- 。。。。。。

因为涉及到解密，样本中使用对称加密算法居多，加密与解密的密钥相同，常见的对称加密算法是AES和DES。

AES：AES 是块加密算法，每次处理 128 位（16 字节）的数据块。数据块被分为多个 4x4 的字节矩阵进行处理。x[j]=s[i](https://blog.csdn.net/m0_72827793/article/details/j+i)%4] 循环4次 s[i][j]=x[j] 循环4次 整体循环4次(行位移）

非对称加密是指加密和解密使用密钥不同，一般使用公钥加密，私钥解密，常用openssl。在样本分析中只有提取私钥才能解密文件或者网络流量。

RSA：生成的私钥文件默认开头为“**-----BEGIN OPENSSH PRIVATE KEY-----**”，结尾为“**-----END OPENSSH PRIVATE KEY-----**”，内容可能使用**=**号补位



## 0x3、解密方法

案列

看一个Windows程序样本，抓包发现它接收指令，然后返回加密的数据到cc，解析它的加解密算法。

![img](https://image-1253434595.cos.ap-chengdu.myqcloud.com/img/202409201650671.webp)

使用工具

- IDA pro静态，静态分析反编译工具（插件工具如FindCrypt2、KANAL）
- Ollydebug，动态分析反编译工具

步骤

1、使用PEID的KANAL插件查看发现AES加密特征（RIJNDAEL算法就是AES）

![image-20240914154454506](https://image-1253434595.cos.ap-chengdu.myqcloud.com/img/202409201650330.png)

2、跳转面的地址00401E95所在的函数，发现大量计算，代码中出现 4*的计算，疑似AES加密块

![image-20240914163824922](https://image-1253434595.cos.ap-chengdu.myqcloud.com/img/202409201650818.png)

3、交叉应用发现，该函数在main中被调用，调用push将key压栈，所以AES加密key就是16字节的字符串"ijklmnopqrstuvwx"

![image-20240914165119038](https://image-1253434595.cos.ap-chengdu.myqcloud.com/img/202409201650132.png)

4、main函数，初始化网络与cc建立连接socks，由sub_401AC2加密，sub_4015B7主要实现管道的输入和输出、进程线程创建。

![image-20240914174825707](https://image-1253434595.cos.ap-chengdu.myqcloud.com/img/202409201650331.png)

编写流量解密脚本，拷贝流量中返回的载荷

![image-20240918154634103](https://image-1253434595.cos.ap-chengdu.myqcloud.com/img/202409201650978.png)

![image-20240918142902735](https://image-1253434595.cos.ap-chengdu.myqcloud.com/img/202409201650734.png)

脚本如下，但解出来编码不知道为什么很奇怪，使用x32dbg调式看看

![image-20240918144621293](https://image-1253434595.cos.ap-chengdu.myqcloud.com/img/202409201650381.png)



另外还有其他方法，加密函数处下断点，找到缓冲区的数据buf，然后将它们提取出来，利用winHex进行复制另存到一个文件中，然后再与加密后的数据进行对比，后面再研究看看。
