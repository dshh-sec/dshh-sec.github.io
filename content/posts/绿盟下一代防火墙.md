+++
title = '绿盟下一代防火墙取证'
date = 2023-03-20T15:03:29+08:00
draft = false

+++
绿盟下一代防火墙取证，需要进入到系统后台，可以使用ssh登录

用户名：develop

密码：绿盟厂家支持



ls -alhR /proc/*/exe 2> \dev\null |grep deleted

根据木马1、会自删除得到特点，2、随机8个字母数字。可以通过一个简单的ls和grep命令进行检测。此命令将遍历/proc目录中所有正在运行的进程，并检查它们是否具有格式为memfd:（deleted）的可执行路径。这个条目非常可疑，并且在无文件攻击中很常见。

![](https://image-1253434595.cos.ap-chengdu.myqcloud.com/img/202409191515150.png)

netstat -nalp

ps -auxw

cp /proc/<PID>/exe /tmp/exe.bin

/mnt/cf/orig/nsfocus/bin/随机8个字母数字



找到确认pid后，使用以下脚本转储pid内存，脚本依赖gdb（若无gdb可静态编译）。

```bash
#! /bin/env bash
PID=$1
# Temporary file to append memory to
TMP="`pwd`/$(date +"%Y-%m-%d")_${PID}.dump"

# Reads memory addresses from /proc/<PID>/maps
memory_addresses=$(cat /proc/${PID}/maps | sed -n 's/^\([0-9a-f]*\)-\([0-9a-f]*\) .*$/\1\t\2/p')

# Inform user about acquisition
echo -e "$(date +"%Y-%m-%d %H:%M:%S")\tStarting acquision of process ${PID}" >&2
echo -e "$(date +"%Y-%m-%d %H:%M:%S")\tProc cmdline: \"$(cat /proc/$PID/cmdline)\"" >&2
# Loops over the retrieved memory areas and dumps their content to a temporary file
echo "${memory_addresses}" | while read start stop;
do
echo -e "$(date +"%Y-%m-%d %H:%M:%S")\tDumping $start - $stop" >&2
gdb --batch --pid ${PID} -ex "append memory ${TMP} 0x$start 0x$stop" >/dev/null 2>&1
done
# Calculates the hash of the retrieved contents
echo -e "$(date +"%Y-%m-%d %H:%M:%S")\tResulting SHA512: $(sha512sum ${TMP} | cut -d' ' -f1 -)" >&2
```



ps

默认ssh 50022端口（需要web管理开启远程支撑）

![](https://image-1253434595.cos.ap-chengdu.myqcloud.com/img/202409191515487.png)



用户

develop（管理员）

engineer

![](https://image-1253434595.cos.ap-chengdu.myqcloud.com/img/202409191515602.png)

版本

![](https://image-1253434595.cos.ap-chengdu.myqcloud.com/img/202409191515741.png)

Linux version 3.16.35-nsfocus.x86_64 (chenshi2@localhost.localdomain) (gcc version 4.8.5 20150623 (Red Hat 4.8.5-36) (GCC) ) #10 SMP PREEMPT Thu Mar 4 16:43:17 CST 2021



内置dns解析（host文件）

122.14.62.70 update.nsfocus.com

125.97.242.141 update.api.nsfocus.com

221.122.179.52 auth.api.nsfocus.com

125.97.242.187 portal.nsfocus.com

221.122.179.32 reputation.api.nsfocus.com

221.122.179.5 espp.api.nsfocus.com







